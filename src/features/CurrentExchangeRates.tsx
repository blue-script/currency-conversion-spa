import React from "react"
import { instance } from "../common/instance"

export const CurrentExchangeRates: React.FC = (props: any) => {
  return (
    <>
      instance
    </>
  )
}

// export default CurrentExchangeRates